<nav class="navbar navbar-expand-lg navbar navbar-dark bg-primary">
    <a href="{{ path('home') }}">
        <img alt="logo" src="{{ asset('assets/logo.png') }}" class="logo">
    </a>
    <div class="collapse navbar-collapse " id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('about') }}">A propos de nous <span
                            class="sr-only">(current)</span></a>
            </li>
            {% if app.user %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('project_index') }}">Projets<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('team_index') }}">Equipes<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('user_edit', {'id': app.user.id, 'isEditingTeam': 'false', 'editedTeamId': -1}) }}">Profil<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item active ">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter<span
                                class="sr-only">(current)</span></a>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('register') }}">S'enregistrer <span
                                class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item active ">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion <span class="sr-only">(current)</span></a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>